<?xml version="1.0"?>
<project name="checkersPrototype" default="compile" basedir=".">
	<property name="src" value="src"/>
	<property name="build" value="build"/>
	
		<property name="build.classes" value="${build}/classes"/>
  		<property name="build.lib" value="${build}/lib"/>

	<target name="init" >
		<mkdir dir="${build}"/>
		<mkdir dir="${build.classes}"/>
		<mkdir dir="${build.lib}"/>
	</target>
	
	<target name="compile" depends="init">
		<javac srcdir="${src}" destdir="${build.classes}"/>
	</target>
	
	<target name="clean" description="Remove all generated files.">
		<delete dir="${build}"/>
	</target>
	
	<target name="jar" depends="compile" description="Generates all executables.">
		<jar jarfile="${build.lib}/Checkers.jar" manifest="manifest.mf"  basedir="${build.classes}"/>
	</target>
</project>
